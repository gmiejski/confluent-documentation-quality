# Confluent documentation quality

Why?

## Setup:

* `virtualenv confluent-documentation-quality`
* `source confluent-documentation-quality/bin/activate`
* `npm install -g electron@1.8.4 orca`
* `pip install -r requirements.txt`


# usage

* `make save_today`
* `make publish`